local InitCommand = {
    Name = "init",
    Description = "initalize a new package",
    Usage = "init [directory]",
}

local function Init()
    
end

return {
    InitCommand = InitCommand,
    Init = Init,
}