local UpdateCommand = {
    Name = "update",
    Description = "update the package manager",
    Usage = "update",
}

local function Update()
    
end

return {
    UpdateCommand = UpdateCommand,
    Update = Update,
}