local InstallCommand = {
    Name = "install",
    Description = "install package from lpm repository",
    Usage = "install <package>"
}

local function Install()
    
end

return {
    InstallCommand = InstallCommand,
    Install = Install
}